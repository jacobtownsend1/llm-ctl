# No accelerator. Slow, but it works on a laptop, and it is a good way to check
# a definition before moving it to the machine with the GPU.
DESC="Qwen2.5 1.5B Instruct on CPU"
BACKEND="vllm"
IMAGE="vllm/vllm-openai:latest"
HF_MODEL="Qwen/Qwen2.5-1.5B-Instruct"
MODEL_GPUS="none"
MODEL_PORT=8001

ARGS=(--max-model-len 8192 --dtype float32)
